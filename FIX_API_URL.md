# 🔧 Исправление "Текущий URL: http://localhost:8080"

## Проблема

Фронтенд показывает ошибку: "Текущий URL: http://localhost:8080"

Это означает, что фронтенд не пересобран или использует старую версию.

## Решение

### Вариант 1: Пересобрать фронтенд через GitHub Actions (рекомендуется)

1. **Обновите GitHub Secrets:**
   - GitHub → Settings → Secrets → Actions
   - Добавьте или обновите: `VITE_API` = `https://cma-crypto-miniapp.onrender.com`

2. **Запустите пересборку:**
   - GitHub → Actions → "Deploy to GitHub Pages"
   - Нажмите "Run workflow" → "Run workflow"
   - Дождитесь завершения (1-2 минуты)

3. **Очистите кэш браузера:**
   - Откройте https://ait-prog.github.io/CMA-Crypto-MiniApp/
   - Нажмите Ctrl+Shift+R (жесткая перезагрузка)
   - Или Ctrl+F5

### Вариант 2: Проверка кода

Код уже обновлен - дефолтный URL установлен на:
```
https://cma-crypto-miniapp.onrender.com
```

Но фронтенд нужно пересобрать, чтобы изменения вступили в силу.

## Проверка

После пересборки:

1. Откройте https://ait-prog.github.io/CMA-Crypto-MiniApp/
2. Откройте консоль браузера (F12)
3. Проверьте:
   - Должно быть: `[API] Using API URL: https://cma-crypto-miniapp.onrender.com`
   - НЕ должно быть: `localhost:8080`

4. Проверьте Network вкладку:
   - Запросы должны идти на `cma-crypto-miniapp.onrender.com`
   - НЕ на `localhost`

## Если все еще видите localhost:8080

1. Убедитесь, что GitHub Secrets установлен:
   - `VITE_API` = `https://cma-crypto-miniapp.onrender.com`

2. Проверьте, что workflow выполнился:
   - Actions → последний запуск → должен быть зеленый

3. Очистите кэш браузера:
   - Ctrl+Shift+R или Ctrl+F5

4. Проверьте в консоли браузера:
   - `console.log("[API] Using API URL:", API)` должен показать Render URL

