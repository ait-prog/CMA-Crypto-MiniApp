# ⚡ Быстрый деплой API на Railway

## За 5 минут

### 1. Регистрация на Railway
- Перейдите на https://railway.app
- Зарегистрируйтесь через GitHub

### 2. Создание проекта
1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий: `ait-prog/CMA-Crypto-MiniApp`
4. Нажмите "Deploy Now"

### 3. Настройка
1. В настройках проекта:
   - **Root Directory**: `api`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### 4. Переменные окружения
В разделе "Variables" добавьте:
```
CRYPTOPANIC_KEY=ecef6293b2242fb90b6e03175ecb5a4a2e3a3b01
```

### 5. Получение URL
1. Railway создаст публичный URL автоматически
2. Скопируйте URL (например: `https://crypto-api-production.up.railway.app`)

### 6. Обновление фронтенда

#### Вариант A: Через GitHub Secrets
1. GitHub → Settings → Secrets → Actions
2. Добавьте: `VITE_API` = ваш Railway URL
3. Push в репозиторий → автоматический пересборка

#### Вариант B: Временно в коде (для теста)
Обновите `web/src/api.js`:
```javascript
const API = "https://your-railway-url.railway.app";
```

### 7. Проверка
```bash
curl https://your-railway-url.railway.app/healthz
curl https://your-railway-url.railway.app/coins
```

## Готово! 🎉

Теперь ваш API доступен публично и приложение в Telegram будет работать!

