# ⚡ Быстрый старт Railway - Пошагово

## Шаг 1: Создание проекта

1. Откройте https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйтесь через GitHub (если нужно)
5. Выберите репозиторий: **ait-prog/CMA-Crypto-MiniApp**
6. Нажмите **"Deploy Now"** или **"Add Service"**

**Важно:** Railway подключит весь репозиторий (main). Это нормально!

## Шаг 2: Настройка Root Directory ⚠️ ОБЯЗАТЕЛЬНО!

После создания проекта:

1. **В Railway Dashboard** найдите ваш проект
2. **Кликните на сервис** (квадрат с названием проекта)
3. Вверху вы увидите вкладки: **Metrics** | **Deployments** | **Settings**
4. Перейдите во вкладку **Settings**
5. Прокрутите вниз до раздела **Source**
6. Найдите поле **Root Directory**
7. **Введите:** `api` (просто `api`, без слешей)
8. Нажмите **Save** или **Update**

**Проверка:**
- Railway автоматически перезапустит деплой
- В Build Logs должно быть видно файлы из папки `api/`
- В логах должно быть: `Working directory: /app` (это папка api)

## Шаг 3: Переменные окружения

1. В настройках сервиса (Settings)
2. Найдите раздел **Variables**
3. Нажмите **"New Variable"**
4. Добавьте:
   - **Name:** `CRYPTOPANIC_KEY`
   - **Value:** `ecef6293b2242fb90b6e03175ecb5a4a2e3a3b01`
5. Нажмите **Add**

(Опционально, если нужны):
- `LLM4WEB3_URL` - URL вашей LLM модели
- `LLM4WEB3_TOKEN` - токен для LLM

## Шаг 4: Проверка деплоя

1. Railway автоматически начнет сборку после сохранения Root Directory
2. Дождитесь завершения (Build → Deploy → Running)
3. В настройках сервиса найдите раздел **Networking**
4. Нажмите **"Generate Domain"** (если домен не создан автоматически)
5. Скопируйте публичный URL (например: `https://crypto-api-production.up.railway.app`)

## Шаг 5: Проверка работы API

Откройте в браузере или через curl:

```bash
# Health check
curl https://your-railway-url.railway.app/healthz

# Список монет
curl https://your-railway-url.railway.app/coins

# Цена Bitcoin
curl https://your-railway-url.railway.app/price/bitcoin

# Новости Bitcoin
curl https://your-railway-url.railway.app/news/bitcoin
```

Должны получить ответы (не ошибки).

## Шаг 6: Обновление фронтенда

1. Откройте GitHub: https://github.com/ait-prog/CMA-Crypto-MiniApp
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте:
   - **Name:** `VITE_API`
   - **Value:** ваш Railway URL (например: `https://crypto-api-production.up.railway.app`)
5. Сохраните

6. Перейдите во вкладку **Actions**
7. Выберите workflow **"Deploy to GitHub Pages"**
8. Нажмите **"Run workflow"** → **"Run workflow"**
9. Дождитесь завершения

## Готово! 🎉

Теперь приложение в Telegram будет работать с публичным API!

## Troubleshooting

### Railway не находит main.py
- **Проблема:** Root Directory не установлен
- **Решение:** Установите Root Directory = `api` в Settings

### Ошибка "Module not found"
- **Проблема:** Railway не может найти модули
- **Решение:** Убедитесь, что Root Directory = `api`

### API не отвечает
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что порт правильный (Railway использует $PORT)

### Новости пустые
- Проверьте, что `CRYPTOPANIC_KEY` установлен
- Developer план имеет задержку 24 часа
- Проверьте логи API в Railway

