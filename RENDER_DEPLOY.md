# 🚀 Деплой API на Render (бесплатно)

Render - отличная альтернатива Railway, бесплатный тариф для хобби-проектов.

## Шаг 1: Регистрация

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub
4. Подтвердите email

## Шаг 2: Создание Web Service

1. В Dashboard нажмите **"New +"**
2. Выберите **"Web Service"**
3. Нажмите **"Connect GitHub"** (если еще не подключен)
4. Выберите репозиторий: **ait-prog/CMA-Crypto-MiniApp**

## Шаг 3: Настройка сервиса

### Основные настройки:

- **Name**: `crypto-api` (или любое название)
- **Region**: `Frankfurt (EU)` или `Oregon (US)` (ближайший к вам)
- **Branch**: `main`
- **Root Directory**: `api` ⚠️ **ВАЖНО!**
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### Environment Variables:

Нажмите **"Add Environment Variable"** и добавьте:

- **Key**: `CRYPTOPANIC_KEY`
- **Value**: `ecef6293b2242fb90b6e03175ecb5a4a2e3a3b01`

(Опционально):
- **Key**: `LLM4WEB3_URL`
- **Value**: (оставьте пустым или укажите ваш URL)

- **Key**: `LLM4WEB3_TOKEN`
- **Value**: (оставьте пустым или укажите токен)

## Шаг 4: План

Выберите **"Free"** план (достаточно для начала).

## Шаг 5: Создание

1. Нажмите **"Create Web Service"**
2. Render автоматически начнет деплой
3. Дождитесь завершения (Build → Deploy → Running)
4. Скопируйте публичный URL (например: `https://crypto-api.onrender.com`)

## Проверка работы

После деплоя проверьте:

```bash
curl https://your-app.onrender.com/healthz
curl https://your-app.onrender.com/coins
curl https://your-app.onrender.com/news/bitcoin
```

## После деплоя

1. Скопируйте публичный URL (например: `https://crypto-api.onrender.com`)
2. Обновите GitHub Secrets:
   - GitHub → Settings → Secrets → Actions
   - Добавьте: `VITE_API` = ваш Render URL
3. Пересоберите фронтенд (автоматически через GitHub Actions)

## Преимущества Render

- ✅ Бесплатный тариф для хобби-проектов
- ✅ Простая настройка через веб-интерфейс
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка Python/FastAPI
- ✅ Автоматические SSL сертификаты

## Troubleshooting

### Ошибка: "Module not found"
- Проверьте, что Root Directory = `api`
- Проверьте, что requirements.txt содержит все зависимости

### Ошибка: "Port already in use"
- Используйте `--port $PORT` в Start Command
- Render автоматически установит переменную PORT

### API не отвечает
- Проверьте логи в Render Dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что Start Command правильный

