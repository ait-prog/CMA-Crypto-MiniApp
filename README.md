# Telegram Crypto Mini App

MVP телеграм мини-приложения для анализа криптовалют с тремя вкладками: Новости, Графики, Риски и тенденции.

 **Live Demo**: https://ait-prog.github.io/CMA-Crypto-MiniApp/

##  Структура проекта

```
crypto-miniapp/
├── api/              # FastAPI бэкенд
├── web/              # React фронтенд
├── bot/              # Telegram бот
└── docker-compose.yml
```

##  Быстрый старт

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните полученный `BOT_TOKEN`

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
WEBAPP_URL=http://localhost:5173
CRYPTOPANIC_KEY=your_cryptopanic_key_optional
LLM4WEB3_URL=http://localhost:8001
LLM4WEB3_TOKEN=your_llm_token_optional
```

### Шаг 3: Запуск через Docker Compose

```bash
docker-compose up --build
```

### Деплой на GitHub Pages

Для деплоя фронтенда на GitHub Pages см. [DEPLOY.md](DEPLOY.md)

Или запуск компонентов отдельно:

#### Бэкенд (API)
```bash
cd api
pip install -r requirements.txt
uvicorn main:app --reload --port 8080
```

#### Фронтенд (WebApp)
```bash
cd web
npm install
npm run dev
```

#### Бот
```bash
cd bot
pip install -r requirements.txt
python bot.py
```

##  Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите кнопку "Open App"
4. Используйте поиск для выбора криптовалюты
5. Переключайтесь между вкладками: Новости, Графики, Риски

## API Endpoints

- `GET /healthz` - проверка здоровья сервиса
- `GET /coins` - список поддерживаемых монет
- `GET /price/{coin_id}` - текущая цена монеты
- `GET /ohlc/{coin_id}?days=30` - данные свечей для графика
- `GET /news/{coin_id}` - последние новости
- `POST /analysis/{coin_id}` - технический анализ + LLM4Web3

##  Поддерживаемые криптовалюты

- BTC (Bitcoin)
- ETH (Ethereum)
- SOL (Solana)
- BNB (BNB)
- XRP (Ripple)
- ADA (Cardano)
- DOGE (Dogecoin)
- AVAX (Avalanche)
- TON (TON)
- TRX (TRON)

## 🔗 Интеграция с LLM4Web3

Для подключения вашей LLM4Web3 модели:

1. Установите переменные окружения `LLM4WEB3_URL` и `LLM4WEB3_TOKEN`
2. LLM должен принимать POST запросы в формате:
   ```json
   {
     "coin": "bitcoin",
     "metrics": {
       "rsi": 65.5,
       "ma20": 45000,
       "ma50": 42000,
       "vol30": 0.35,
       "dd30": -0.15
     },
     "news": [...]
   }
   ```
3. LLM должен возвращать:
   ```json
   {
     "summary": "Краткий анализ...",
     "risk_level": "умеренный"
   }
   ```

Если LLM недоступна, используется эвристический анализ на основе технических индикаторов.

##  Дальнейшее развитие

- [ ] WebSockets для realtime цен
- [ ] Больше индикаторов (MACD, ATR)
- [ ] Пользовательские списки монет
- [ ] Алерты на ценовые движения
- [ ] История портфеля

##  Технологии

- **Backend**: FastAPI, Python
- **Frontend**: React, Vite, Lightweight Charts
- **Bot**: python-telegram-bot
- **Data**: CoinGecko API, CryptoPanic API

