# 📍 Root Directory в Render - Как ввести правильно

## ⚠️ Важно: Root Directory - это ТЕКСТОВОЕ ПОЛЕ!

В Render поле **Root Directory** - это **текстовое поле для ввода**, а не выпадающий список!

## Что вы видите:

В настройках Render может быть:
- **Branch**: `main` ← это выпадающий список (выберите main)
- **Root Directory**: `main` или пустое ← это ТЕКСТОВОЕ ПОЛЕ!

## Что нужно сделать:

1. Найдите поле **"Root Directory"** в настройках
2. **Кликните на это поле** (это текстовое поле)
3. Если там написано `main` или что-то другое - **удалите это**
4. **Введите:** `api` (просто `api`, без кавычек, без слешей)
5. Нажмите **Save** или **Create Web Service**

## Визуальная подсказка:

```
Render Dashboard
└── New Web Service
    └── Settings
        ├── Branch: [main ▼] ← выпадающий список (выберите main)
        └── Root Directory: [main] ← ТЕКСТОВОЕ ПОЛЕ!
                                      ↓
                                      УДАЛИТЕ "main"
                                      ↓
                                      ВВЕДИТЕ "api"
```

## Пошагово:

1. **Откройте настройки Web Service**
2. **Найдите поле "Root Directory"**
3. **Кликните на поле** (оно станет редактируемым)
4. **Удалите** текущее значение (если есть)
5. **Введите:** `api`
6. **Сохраните**

## Проверка:

После сохранения:
- Render автоматически перезапустит деплой
- В Build Logs должно быть видно файлы из папки `api/`
- Должно быть: `Working directory: /app` (это папка api)

## Если все еще не работает:

1. Убедитесь, что вы ввели `api` (не `api/`, не `/api`, не `./api`)
2. Проверьте, что сохранили изменения
3. Посмотрите Build Logs - там должны быть файлы из папки api

