# 📝 Пошаговая инструкция: Root Directory в Railway

## ⚠️ Важно: Root Directory - это ТЕКСТОВОЕ ПОЛЕ, не выпадающий список!

Вам нужно **ВВЕСТИ** `api` вручную, а не выбрать из списка.

## Точные шаги:

### Шаг 1: Откройте ваш сервис

1. В Railway Dashboard найдите ваш проект
2. **Кликните на сервис** (квадрат/карточка с названием проекта)
3. НЕ на проект, а на **сервис внутри проекта**

### Шаг 2: Откройте Settings

1. Вверху страницы сервиса вы увидите вкладки:
   - **Metrics**
   - **Deployments**
   - **Settings** ← **КЛИКНИТЕ СЮДА**

### Шаг 3: Найдите Root Directory

1. После открытия Settings прокрутите страницу **ВНИЗ**
2. Найдите раздел **"Source"** или **"Build & Deploy"**
3. В этом разделе будет поле **"Root Directory"**

### Шаг 4: ВВЕДИТЕ `api` вручную

1. **Кликните на поле "Root Directory"** (это текстовое поле)
2. **Введите:** `api` (просто `api`, без слешей, без кавычек)
3. Нажмите **Enter** или кнопку **Save** / **Update**

### Визуальная схема:

```
Railway Dashboard
│
├── Ваш проект (например: "crypto-api")
    │
    └── Service (сервис) ← КЛИКНИТЕ СЮДА
        │
        ├── [Metrics] [Deployments] [Settings] ← КЛИКНИТЕ "Settings"
        │
        └── Settings страница:
            │
            └── Source (прокрутите вниз)
                │
                └── Root Directory: [________] ← ВВЕДИТЕ "api" сюда
```

## Если не видите поле Root Directory:

### Вариант 1: Прокрутите страницу вниз
- Root Directory может быть внизу страницы Settings
- Прокрутите до конца

### Вариант 2: Проверьте раздел "Build & Deploy"
- Иногда Root Directory находится в разделе "Build & Deploy"
- Прокрутите Settings и ищите этот раздел

### Вариант 3: Проверьте, что вы в Settings сервиса
- Убедитесь, что вы открыли Settings **сервиса**, а не проекта
- Вверху страницы должно быть: Metrics | Deployments | Settings

## Альтернативный способ (если не видите поле):

### Через Railway CLI:

1. Установите Railway CLI:
   ```bash
   npm i -g @railway/cli
   ```

2. Авторизуйтесь:
   ```bash
   railway login
   ```

3. Выберите проект:
   ```bash
   railway link
   ```

4. Установите Root Directory:
   ```bash
   railway variables set RAILWAY_ROOT_DIRECTORY=api
   ```

## Проверка после настройки:

1. Railway автоматически перезапустит деплой
2. Перейдите во вкладку **Deployments**
3. Откройте последний деплой
4. Посмотрите **Build Logs**
5. Должно быть видно:
   - `Working directory: /app`
   - Файлы из папки `api/` (main.py, requirements.txt и т.д.)

## Если все еще не работает:

1. Сделайте скриншот страницы Settings
2. Проверьте, что вы в правильном месте
3. Возможно, интерфейс Railway изменился

