# 🚂 Настройка Railway для деплоя API

## Важно: Настройка Root Directory

Railway не может автоматически определить структуру проекта. Нужно указать **Root Directory**:

### Шаг 1: Создание проекта в Railway

1. Перейдите на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите репозиторий: `ait-prog/CMA-Crypto-MiniApp`

### Шаг 2: Настройка Root Directory

**КРИТИЧЕСКИ ВАЖНО:**

1. После создания проекта откройте настройки сервиса
2. Перейдите в **Settings** → **Service Settings**
3. Найдите **Root Directory**
4. Установите: `api`
5. Сохраните изменения

### Шаг 3: Переменные окружения

В разделе **Variables** добавьте:

```
CRYPTOPANIC_KEY=ecef6293b2242fb90b6e03175ecb5a4a2e3a3b01
LLM4WEB3_URL=
LLM4WEB3_TOKEN=
```

### Шаг 4: Настройка Start Command

В настройках **Deploy** проверьте:

**Start Command:**
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

Railway автоматически установит переменную `$PORT`.

### Шаг 5: Деплой

1. Railway автоматически начнет деплой
2. Дождитесь завершения сборки
3. Скопируйте публичный URL (например: `https://crypto-api-production.up.railway.app`)

## Проверка работы

После деплоя проверьте:

```bash
curl https://your-railway-url.railway.app/healthz
curl https://your-railway-url.railway.app/coins
curl https://your-railway-url.railway.app/news/bitcoin
```

## Troubleshooting

### Ошибка: "Railpack could not determine how to build"
- **Причина**: Не указан Root Directory
- **Решение**: Установите Root Directory = `api` в настройках сервиса

### Ошибка: "Module not found"
- **Причина**: Railway не может найти модули
- **Решение**: Убедитесь, что Root Directory = `api`

### Ошибка: "Port already in use"
- **Причина**: Неправильная команда запуска
- **Решение**: Используйте `--port $PORT` (Railway автоматически установит порт)

### API не отвечает
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что порт правильный

## Структура проекта для Railway

```
CMA-Crypto-MiniApp/
├── api/              ← Root Directory для Railway
│   ├── main.py
│   ├── requirements.txt
│   ├── Procfile      ← Для Railway
│   ├── start.sh      ← Альтернатива
│   └── railway.json  ← Конфигурация
├── bot/
├── web/
└── ...
```

## После деплоя

1. Скопируйте публичный URL API
2. Обновите GitHub Secrets:
   - Settings → Secrets → Actions
   - Добавьте: `VITE_API` = ваш Railway URL
3. Пересоберите фронтенд (автоматически через GitHub Actions)

