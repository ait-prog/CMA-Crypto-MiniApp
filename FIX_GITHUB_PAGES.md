# 🔧 Исправление проблемы с GitHub Pages

## Проблема
На https://ait-prog.github.io/CMA-Crypto-MiniApp/ показывается README.md вместо приложения.

## Решение

### Шаг 1: Проверьте настройки GitHub Pages

1. Перейдите в репозиторий: https://github.com/ait-prog/CMA-Crypto-MiniApp
2. Откройте **Settings** → **Pages**
3. В разделе **Source** должно быть:
   - **Source**: `GitHub Actions` (не "Deploy from a branch"!)
4. Если указана ветка, измените на **GitHub Actions**
5. Сохраните изменения

### Шаг 2: Запустите workflow вручную

1. Перейдите в **Actions** вкладку
2. Выберите workflow "Deploy to GitHub Pages"
3. Нажмите **Run workflow** → **Run workflow**
4. Дождитесь завершения

### Шаг 3: Проверьте логи workflow

Если workflow падает с ошибкой:

1. Откройте последний запуск workflow
2. Проверьте логи на ошибки
3. Частые проблемы:
   - Отсутствует `package-lock.json` → выполните `npm install` в `web/`
   - Ошибки сборки → проверьте `web/package.json`
   - Ошибки пути → проверьте `web/vite.config.js`

### Шаг 4: Проверьте файл .nojekyll

Убедитесь, что файл `web/.nojekyll` существует (он должен быть в репозитории).

### Шаг 5: Очистите кэш

Если проблема сохраняется:

1. В настройках GitHub Pages временно отключите и включите снова
2. Или удалите и заново создайте workflow

## Проверка

После исправления:
1. Подождите 1-2 минуты
2. Откройте https://ait-prog.github.io/CMA-Crypto-MiniApp/
3. Должно показаться приложение, а не README

## Если все еще не работает

Проверьте:
- [ ] GitHub Pages настроен на **GitHub Actions**
- [ ] Workflow успешно выполнился (зеленая галочка)
- [ ] Файл `web/.nojekyll` существует
- [ ] В `web/vite.config.js` установлен `base: '/CMA-Crypto-MiniApp/'`
- [ ] В `web/package.json` есть скрипт `build`

