# 🔍 Диагностика HTTP 500 ошибок

## Проблема

API возвращает ошибку HTTP 500 без конкретного сообщения.

## Возможные причины

### 1. Render API в режиме сна (Free Tier)

Render на бесплатном плане переводит сервисы в режим сна после 15 минут бездействия.

**Решение:**
- Подождите 30-60 секунд после первого запроса
- Обновите страницу (F5)
- Или используйте платный план Render

### 2. Проблема с CoinGecko API

CoinGecko API может быть недоступен или возвращать ошибки.

**Проверка:**
```bash
curl https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd
```

### 3. Проблема с зависимостями

Могут быть проблемы с установкой зависимостей на Render.

**Проверка:**
- Проверьте логи на Render
- Убедитесь, что `requirements.txt` содержит все зависимости
- Убедитесь, что Python версия правильная (3.11)

### 4. Проблема с импортом сервисов

Если сервисы не загрузились правильно, API может возвращать 500.

**Решение:**
- Проверьте логи на Render на наличие ошибок импорта
- Убедитесь, что все файлы в папке `api/` присутствуют

## Что сделано

1. ✅ Улучшены сообщения об ошибках на фронтенде
2. ✅ Новости теперь не критичны - возвращают пустой список
3. ✅ Добавлено логирование для диагностики
4. ✅ Улучшена обработка ошибок в API

## Диагностика

### Шаг 1: Проверьте доступность API

```bash
# Проверка health check
curl https://cma-crypto-miniapp.onrender.com/healthz
# Должно вернуть: {"ok":true}

# Проверка списка монет
curl https://cma-crypto-miniapp.onrender.com/coins
# Должно вернуть список монет
```

### Шаг 2: Проверьте логи на Render

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Откройте вкладку "Logs"
4. Проверьте наличие ошибок:
   - `[ERROR]` - критические ошибки
   - `[WARNING]` - предупреждения
   - `[API]` - информационные сообщения

### Шаг 3: Проверьте переменные окружения

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Откройте вкладку "Environment"
4. Проверьте, что все переменные установлены:
   - `PORT` (автоматически)
   - `CRYPTOPANIC_KEY` (опционально)
   - `LLM4WEB3_URL` (опционально)
   - `LLM4WEB3_TOKEN` (опционально)

### Шаг 4: Проверьте Root Directory

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Откройте настройки
4. Проверьте, что "Root Directory" установлен на `api`

## Если ошибка сохраняется

1. Проверьте логи на Render для детальной информации
2. Попробуйте перезапустить сервис на Render
3. Проверьте, что все зависимости установлены правильно
4. Убедитесь, что Root Directory установлен на `api`

## Альтернативные решения

### 1. Использовать платный план Render

Платный план не переводит сервисы в режим сна.

### 2. Использовать другой сервис

- **Fly.io** - бесплатный план с постоянной работой
- **Railway** - платный план, но более надежный
- **Heroku** - платный план с хорошей поддержкой

### 3. Использовать keep-alive

Настроить периодические запросы к API, чтобы он не засыпал (не рекомендуется для production).

