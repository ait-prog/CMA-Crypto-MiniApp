# 🚀 Быстрый старт за 5 минут

## Минимальные требования

- Python 3.11+
- Node.js 18+
- Docker и Docker Compose (опционально)

## Быстрая установка (без Docker)

### 1. Бэкенд (2 минуты)

```bash
cd api
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

pip install -r requirements.txt
uvicorn main:app --reload --port 8080
```

Проверьте: http://localhost:8080/healthz

### 2. Фронтенд (2 минуты)

Откройте новый терминал:

```bash
cd web
npm install
npm run dev
```

Проверьте: http://localhost:5173

### 3. Бот (1 минута)

Откройте новый терминал:

```bash
cd bot
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

pip install -r requirements.txt
```

Создайте `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_botfather
WEBAPP_URL=http://localhost:5173
```

Запустите:
```bash
python bot.py
```

### 4. Настройка бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и создайте бота
3. Скопируйте токен в `.env`
4. Отправьте `/newapp` → выберите бота → укажите URL: `http://localhost:5173`
   (для продакшена используйте HTTPS через ngrok или домен)

### 5. Тестирование

1. Найдите вашего бота в Telegram
2. `/start`
3. Нажмите "Open App"
4. Готово! 🎉

## Деплой на GitHub Pages

После настройки GitHub репозитория:

1. Следуйте инструкциям в [GITHUB_SETUP.md](GITHUB_SETUP.md)
2. После деплоя приложение будет доступно:
   **https://ait-prog.github.io/CMA-Crypto-MiniApp/**
3. Обновите `WEBAPP_URL` в боте и `.env`:
   ```env
   WEBAPP_URL=https://ait-prog.github.io/CMA-Crypto-MiniApp/
   ```

## Запуск через Docker (рекомендуется)

```bash
# Создайте .env файл с BOT_TOKEN и WEBAPP_URL
docker-compose up --build
```

## Что дальше?

- Прочитайте [SETUP.md](SETUP.md) для подробной инструкции
- Настройте [LLM4Web3 интеграцию](LLM4WEB3_INTEGRATION.md)
- Изучите [README.md](README.md) для полной документации
- Деплой на [GitHub Pages](DEPLOY.md)

## Troubleshooting

**Ошибка: Module not found**
→ Убедитесь, что виртуальное окружение активировано и зависимости установлены

**Ошибка: Port already in use**
→ Измените порты в конфигурации или остановите другие сервисы

**Бот не отвечает**
→ Проверьте BOT_TOKEN в .env файле

**WebApp не открывается**
→ Убедитесь, что фронтенд запущен и URL правильный в BotFather
