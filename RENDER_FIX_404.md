# 🔧 Исправление 404 ошибки в Render

## Проблема: `{"detail":"Not Found"}`

Это означает, что API запущен, но эндпоинты не найдены.

## Возможные причины:

### 1. Неправильная команда запуска
- Проверьте Start Command в Render
- Должно быть: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### 2. Root Directory не установлен
- Убедитесь, что Root Directory = `api`
- Render должен работать с папкой `api/`

### 3. FastAPI не может найти файл main.py
- Проверьте логи в Render Dashboard
- Должно быть видно: `Application startup complete`

## Решение:

### Шаг 1: Проверьте логи в Render

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Перейдите во вкладку **Logs**
4. Посмотрите последние логи:
   - Должно быть: `Application startup complete`
   - Должно быть: `Uvicorn running on http://0.0.0.0:XXXX`

### Шаг 2: Проверьте настройки

В Render Dashboard → Settings проверьте:

1. **Root Directory**: должно быть `api`
2. **Start Command**: должно быть `uvicorn main:app --host 0.0.0.0 --port $PORT`
3. **Build Command**: должно быть `pip install -r requirements.txt`

### Шаг 3: Проверьте эндпоинты

Попробуйте открыть в браузере:

- https://cma-crypto-miniapp.onrender.com/docs (должна открыться документация FastAPI)
- https://cma-crypto-miniapp.onrender.com/healthz (должен вернуть `{"ok": true}`)
- https://cma-crypto-miniapp.onrender.com/coins (должен вернуть список монет)

### Шаг 4: Если не работает

1. В Render Dashboard → Settings
2. Проверьте **Start Command**
3. Убедитесь, что команда правильная:
   ```
   uvicorn main:app --host 0.0.0.0 --port $PORT
   ```
4. Сохраните изменения
5. Render автоматически перезапустит сервис

## Проверка работы:

После исправления проверьте:

```bash
# Health check
curl https://cma-crypto-miniapp.onrender.com/healthz

# Документация API
curl https://cma-crypto-miniapp.onrender.com/docs

# Список монет
curl https://cma-crypto-miniapp.onrender.com/coins

# Цена Bitcoin
curl https://cma-crypto-miniapp.onrender.com/price/bitcoin
```

## Если все еще не работает:

1. Проверьте логи на наличие ошибок
2. Убедитесь, что все зависимости установлены
3. Проверьте, что файл `api/main.py` существует
4. Убедитесь, что Root Directory = `api`

