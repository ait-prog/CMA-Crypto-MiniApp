# 🔧 Исправление "NetworkError when attempting to fetch resource"

## Проблема

Фронтенд показывает ошибку: "NetworkError when attempting to fetch resource"

Это означает, что браузер не может подключиться к API.

## Возможные причины

### 1. Render API в режиме "sleep" (бесплатный план)

Render на бесплатном плане переводит сервисы в режим сна после 15 минут бездействия. Первый запрос после пробуждения может занять 30-60 секунд.

**Решение:**
- Подождите 30-60 секунд после первого запроса
- Обновите страницу (F5)
- Или используйте платный план Render

### 2. CORS проблема

Хотя CORS настроен правильно (`allow_origins=["*"]`), может быть проблема с конкретным доменом.

**Решение:**
- Проверьте консоль браузера (F12) на наличие CORS ошибок
- Убедитесь, что API использует HTTPS

### 3. Неправильный URL

Фронтенд может использовать неправильный URL API.

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте: `[API] Using API URL: https://cma-crypto-miniapp.onrender.com`
3. Если видите другой URL - пересоберите фронтенд

## Что сделано

1. ✅ Добавлена обработка ошибок во все API функции
2. ✅ Улучшены сообщения об ошибках
3. ✅ Добавлено логирование для диагностики
4. ✅ Добавлено сообщение о возможном режиме сна Render

## Проверка

1. Откройте https://ait-prog.github.io/CMA-Crypto-MiniApp/
2. Откройте консоль браузера (F12)
3. Проверьте:
   - `[API] Using API URL: https://cma-crypto-miniapp.onrender.com`
   - Нет ли CORS ошибок
   - Нет ли ошибок сети

4. Если видите NetworkError:
   - Подождите 30-60 секунд
   - Обновите страницу (F5)
   - Проверьте логи Render (если доступны)

## Дополнительные шаги

### Проверка доступности API

```bash
curl https://cma-crypto-miniapp.onrender.com/healthz
# Должно вернуть: {"ok":true}

curl https://cma-crypto-miniapp.onrender.com/coins
# Должно вернуть список монет
```

### Если API не отвечает

1. Проверьте статус сервиса на Render
2. Проверьте логи на Render
3. Убедитесь, что Root Directory установлен на `api`
4. Убедитесь, что переменные окружения установлены

## Альтернативные решения

### 1. Использовать платный план Render

Платный план не переводит сервисы в режим сна.

### 2. Использовать другой сервис

- **Fly.io** - бесплатный план с постоянной работой
- **Railway** - платный план, но более надежный
- **Heroku** - платный план с хорошей поддержкой

### 3. Использовать keep-alive

Настроить периодические запросы к API, чтобы он не засыпал (не рекомендуется для production).

